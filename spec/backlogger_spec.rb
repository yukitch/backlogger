require "spec_helper"

describe Backlogger do
  it "has a version number" do
    expect(Backlogger::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
